# AI-Robot-Demo 学习记录

## 2026-08-03 第一阶段：搭建并跑通 AI 机器人大脑

### 本次完成的功能

1. 项目骨架：main.py / llm.py / robot.py / README.md / requirements.txt / .env
2. DeepSeek 真实模式 + 离线 Mock 模式，启动时自动切换
3. 桌面快捷方式"AI机器人Demo"，双击启动
4. 接入真实 DeepSeek API Key，口语化指令测试通过

### 核心原理（理解而不是只会运行）

- **三层架构**：理解层（llm.py）→ 契约层（JSON）→ 执行层（robot.py）。
- **为什么用 JSON**：自然语言是模糊的，机器只能执行确定的结构化指令。JSON 就是人和机器之间的"翻译契约"，这也对应真实机器人中的消息格式（如 ROS2 的 msg）。
- **Prompt 设计是关键**：system prompt 限定了 AI 的角色、动作集合、输出格式，让大模型从"聊天"变成"可编程工具"。response_format 强制输出 JSON，temperature 降低随机性。
- **接口一致才能替换**：MockPlanner 与 DeepSeekPlanner 接口相同（都是 plan_task），所以没有 Key 也能测试；未来 robot.py 换成 ROS2 控制器时，main.py 也不用改。
- **世界模型**：robot.py 里的 workspace 字典就是机器人的"世界状态"，机械臂位置和夹爪是状态变量——这是机器人编程的核心思想。

### 修改记录

| 时间 | 提交 | 说明 |
| --- | --- | --- |
| 2026-08-03 | 3442240 | first AI robot demo |
| 2026-08-03 | a9b039f | add desktop launcher and auto mode detection |
| 2026-08-03 | 2b5a463 | feat: V2 memory system（Memory 记忆系统） |
| 2026-08-03 | 8fcea76 | feat: V3 ROS2 robot framework |
| 2026-08-03 | e07d74f | feat: V4 gazebo simulation and vision |

### 验证结果

- `把红色零件移动到检测区` → `{"action": "move", "object": "红色零件", "target": "检测区"}` 执行成功
- 口语化指令 `帮我把那个红色的零件拿到做检测的地方去` → DeepSeek 正确理解并规划成功

### 下一步

- 上传 GitHub ✅（已推送）
- 阶段 2：给机器人加记忆（状态持久化）和环境信息 ✅

## 2026-08-03 第二阶段：Memory 记忆系统（V2）

### 本次完成的功能

1. `memory.py`：SQLite 记忆系统（保存 / 查询 / 格式化给 AI）
2. `main.py` 新流程：用户输入 → 记忆查询 → 记忆 + 任务发给 DeepSeek → 动作 → 执行
3. `llm.py`：system prompt 升级为带 `{memory}` 记忆上下文的模板
4. `robot.py`：新增 `add_station()`，让机器人学会记忆中的新位置
5. `database.db`：自动生成，记忆跨会话持久化

### 核心原理（理解而不是只会运行）

- **SQLite 是什么**：轻量级嵌入式数据库，所有数据存在单个 `.db` 文件里，Python 内置 sqlite3 模块直接操作，不需要安装数据库服务。适合单机小项目。
- **记忆检索怎么做**：用 `LIKE` 模糊匹配主题和内容，还支持"提问里包含主题"的反向匹配，所以 `把零件送到A区域` 能查到 A区域 的记忆。
- **AI Agent 为什么需要 Memory**：大模型本身没有记忆，每次对话都是独立的。记忆 = 外部存储 + 上下文注入，让 AI 结合历史知识和环境信息做决策。
- **上下文注入原理**：把查询到的记忆格式化成一两行文本，塞进 system prompt 的 `{memory}` 位置，AI 就"知道"环境了。
- **架构意义**：V2 形成了 Agent 的完整一环：记忆（Memory）→ 规划（LLM）→ 执行（Robot）。未来 ROS2 阶段，记忆层可以变成传感器/环境信息的存储和查询。

### 验证结果

- `记住：A区域在生产线左侧` → 保存成功，机器人学会新位置
- `把零件送到A区域` → 检索到记忆，AI 生成 `move` 指令，机器人执行成功
- 关掉程序重新打开 → 机器人自动"回忆起"A区域（数据库持久化）
- V1 旧指令（移动/扫描/放置）全部回归通过

## 2026-08-03 第三阶段准备：ROS2 开发环境搭建

### 已安装环境

- WSL2 + Ubuntu 22.04（虚拟机磁盘位于 `D:\AI-Robot\WSL\Ubuntu-22.04`）
- ROS2 Humble（280 个包：turtlesim / rviz2 / gazebo 11 / colcon）
- Linux 用户 `zlx06`（sudo 权限），软件源已换清华 TUNA 镜像
- SSH 推送 GitHub 改走 443 端口（不再受直连干扰）

### 安装要点

- Ubuntu 镜像下载到 `D:\AI-Robot\Software`，用 `wsl --import` 导入到 D 盘
- 启用 Windows"虚拟机平台"功能（需管理员 + 重启）
- ROS2 用清华镜像安装，全程约 15 分钟

### 下一步

- V3 学习路线：turtlesim 通信演示 → 话题/服务/动作 → 用 Python 写自定义节点

## 2026-08-03 第三阶段：ROS2 Robot Framework（完成）

### 完成内容

- 新建 ROS2 功能包 `ai_robot`（WSL 中 `/home/zlx06/ros2_ws`）：
  - `brain_node`：AI 大脑节点（记忆查询 + DeepSeek 规划）
  - `robot_controller`：机器人控制器节点（世界模型执行）
  - `task_cli`：任务输入终端
  - `demo.launch.py`：一键启动文件
- 复用 V1/V2 的 `llm.py` / `memory.py` / `robot.py`（一行未改）
- 话题：`/ai_robot/task`（任务）、`/ai_robot/action`（JSON 动作）、`/ai_robot/status`（状态）

### 核心概念（ROS2）

- **节点 Node**：独立的可执行程序，每个节点只干一件事
- **话题 Topic**：发布-订阅通信，一个节点发布、其他节点订阅
- **启动文件 Launch**：一条命令启动多个节点

### 验证结果

- `把红色零件移动到检测区` → 记忆检索 → DeepSeek 规划 → 控制器执行 → 检测区出现红色零件 ✅
- `记住：A区域在生产线左侧` → 记忆保存 + 机器人学会新位置 ✅
- `把零件送到A区域` → 结合记忆规划并执行成功 ✅

## 2026-08-03 第四阶段：Gazebo 仿真 + 视觉感知（完成）

### 完成内容

- Gazebo 仿真世界：差速轮机器人（带相机）+ 红/蓝/绿彩色零件
- `vision_node`：OpenCV HSV 颜色识别，检测结果写入记忆库
- AI 大脑结合视觉记忆规划（问"红色零件在哪里"能查到视觉识别结果）

### 关键知识点

- **URDF**：描述机器人几何、关节、传感器的 XML（Gazebo 仿真机器人的"身份证"）
- **Gazebo 传感器**：相机传感器发布 `/camera/image_raw`，视觉节点订阅处理
- **OpenCV 颜色识别**：RGB 转 HSV → 颜色范围阈值 → 轮廓提取 → 定位零件
- **踩过的坑**：Gazebo 相机默认朝 +X 看，需绕 Y 轴转 90° 才朝下；传感器要显式写 `<pose>`，否则固定关节合并会丢姿态

### 验证结果

- 相机画面识别出红/蓝/绿三个零件及左右位置 ✅
- 视觉结果写入记忆：`红色零件 -> 右侧区域（视觉识别）` ✅
- `红色零件在哪里` → AI 检索视觉记忆并输出 scan 动作 ✅

### 追加：让机器人在 Gazebo 里真的动起来

- robot_controller 增加导航控制：订阅 /odom 获取位置，发布 /cmd_vel 驱动差速机器人
- 控制逻辑：先转向目标方向（比例控制 + 阻尼），再直行，距离小于 0.25m 判定到达
- 实测：`把红色零件移动到检测区` → 机器人 4 秒内开到检测区并日志确认"已到达目标位置"
- 踩过的坑：
  - 两轮机器人底盘会前倾栽倒 → 加前后万向轮支撑
  - 万向轮是固定球体不能滚动 → 设零摩擦让它滑动
  - 零件放在工位上会挡住机器人 → 零件改纯视觉模型（真实抓取留给 V5）

## 2026-08-04 第五阶段启动：同步新路线图

### 本次完成的功能

1. 更新项目路线图：V5 由「真实机械臂 / MoveIt」改为「AI Robot Agent 科研完善」（进行中）
2. 新增 V6「ABB RobotStudio / 真实机器人」、V7「工业智能制造应用」（规划中）
3. 同步 README.md 与 Notes/项目交接文档.md 中的路线图和下一步计划

### 下一步

- 明确 V5「AI Robot Agent 科研完善」的具体内容后开始开发

## 2026-08-04 V5.1：Streamlit GUI 图形界面（完成）

### 开发顺序与产物

**第一步：创建 gui 目录和基础文件结构**

1. 修改文件列表：新增 `gui/config.py`、`gui/requirements.txt`、`gui/README.md`、`gui/app.py`（骨架）、`gui/backend.py`（骨架）、`gui/ros2_client.py`（骨架）
2. 文件作用：目录骨架 + 路径/话题常量 + 后端抽象接口（AgentBackend / Ros2Backend / LocalBackend）
3. 如何测试：`python -m py_compile gui/*.py`，全部通过
4. 学习笔记：先定义接口再实现，界面层只依赖 AgentBackend 的方法，后续加 Local 模式不用改界面
5. Git 提交：`28046d9 feat(gui): add V5.1 GUI skeleton structure (streamlit scaffold)`

**第二步：实现 ROS2 客户端通信**

1. 修改文件列表：`gui/ros2_client.py`（完整实现）、`gui/backend.py`（Ros2Backend 实现）、新增 `gui/test_ros2_client.py`
2. 文件作用：后台线程 spin 的 rclpy 客户端（发 task、收 status/vision/odom），Ros2Backend 统一后端接口 + 记忆只读
3. 如何测试：WSL 起 `demo.launch.py` → `python3 gui/test_ros2_client.py`，收到「检索到记忆 → 工作台 JSON」完整回路，退出码 0
4. 学习笔记：
   - rclpy 回调用「独立线程 spin + 锁 + 快照」模式，主线程只读最新状态，避免与 Streamlit rerun 冲突
   - 状态去重要用时间戳而不是内容（内容会重复发布）
   - 进程退出前必须 join spin 线程，否则报 `terminate called without an active exception`
5. Git 提交：`290fab2 feat(gui): ROS2 client backend for task/status/vision/odom (V5.1 step 2)`

**第三步：实现 Streamlit 界面**

1. 修改文件列表：`gui/app.py`（完整界面）、新增 `gui/test_app.py`（AppTest 冒烟测试）、`启动GUI界面.bat`
2. 文件作用：五个标签页（任务对话/工作台/记忆/视觉/机器人状态），从 Windows 一键启动 WSL 里的 streamlit
3. 如何测试：AppTest 渲染零异常；真实启动 `python3 -m streamlit run gui/app.py --server.headless true`，curl 返回 HTTP 200
4. 学习笔记：
   - `streamlit run` 前必须先 `source /opt/ros/humble/setup.bash`，否则 import rclpy 失败
   - streamlit 装完在 `~/.local/bin` 不在 PATH，统一用 `python3 -m streamlit`
   - 安装 streamlit 时锁定 `numpy==1.26.4`，防止被升到 2.x 破坏 cv_bridge
   - WSL 后台长任务不要用 nohup（会话结束会被杀），前台跑最稳
5. Git 提交：见下方 V5.1 完成提交

### 验证结果

- ROS2 客户端与 V3 节点实机联调通过（任务下发 → AI 规划 → 世界模型执行 → 状态回传）
- AppTest 冒烟测试：页面渲染 0 异常，进程干净退出
- Streamlit 服务 HTTP 200，浏览器可访问 localhost:8501
- V1-V4 代码零修改

### 下一步

- V5.2 候选：Agent 能力完善（记忆 RAG / 评测 / 多轮对话等）

## 2026-08-04 V5.1 补充：全家桶快捷方式 + 中文乱码彻底解决

### 本次完成的功能

1. 新增 `scripts/` 目录：每个窗口一条独立启动脚本（`run_v1v2.ps1` / `start_ros2_system.sh` / `start_task_cli.sh` / `start_gui.sh`）
2. 新增 `AI_Robot_Demo_All_Launcher.ps1`（UTF-8 BOM）+ `Start_AI_Robot_Demo_All.cmd`：Windows Terminal 一条命令开 4 个标签页
3. 桌面「AI机器人Demo全家桶」快捷方式已更新指向新启动器

### 乱码根因与解决方案

- **根因**：旧启动器用 `cmd.exe /k` 开窗口，cmd 控制台对 UTF-8 中文输出支持不稳定
- **方案**：
  - 全部改用 Windows Terminal（原生 UTF-8），不再用 cmd conhost
  - WSL 侧：`export LANG=C.UTF-8` + `export PYTHONIOENCODING=utf-8`
  - Windows 侧：`$env:PYTHONUTF8=1` + `[Console]::OutputEncoding = UTF8`
  - 所有脚本文件 UTF-8：ps1 带 BOM，.sh 用 LF 行尾
- **大坑**：Windows Terminal 会把命令里的分号当成标签分隔符（**引号内也一样**）→ 复杂命令全部抽到独立脚本，wt 只传脚本路径，彻底避开引号/分号地狱

### 验证结果

- 4 个标签页全部正常：V1/V2 Demo、ROS2 + Gazebo 仿真、任务终端、Streamlit GUI（HTTP 200）
- 中文显示无乱码

### 补充修复：Gazebo 空世界问题

- **现象**：全家桶启动后 Gazebo 里什么都没有（空世界）
- **根因**：上次运行残留的 gzserver 一直占用 11345 端口，新 gzserver 启动失败（exit 255），
  gzclient 连到旧的空世界，spawn_entity 全部失败（exit 1）
- **修复**：`start_ros2_system.sh` 启动前自动 `pkill` 残留的 gzserver/gzclient/旧节点；
  `start_gui.sh` 启动前清理残留 streamlit（防止 8501 端口占用）
- **验证**：重新启动后 4 个 spawn_entity 进程全部 finished cleanly，模型正常生成

## 2026-08-04 V5.1 中文化：界面文字集中管理

### 本次完成的功能

1. 新增 `gui/language.py`：全部用户可见文字集中管理（标题/按钮/提示/状态），app.py 不再散落文本
2. `gui/app.py`：所有 UI 文字改为引用 language.py 常量，业务逻辑零改动

### 中文化的文字（示例）

- `AI Robot Agent` → `AI Robot 智能体`（页面标题 / 侧边栏标题）
- `Connect / Reconnect` → `连接 / 重新连接`
- `Connected` → `已连接：{模式}`
- `ROS2 Mode / Local` → `ROS2 模式 / 本地模式`
- 五个标签页、刷新按钮、输入框占位符、spinner、空状态提示、表格列名、机器人状态指标全部中文化

### 为什么保留英文专业词

AI / Agent / Robot / ROS2 / Gazebo / Streamlit / DeepSeek / JSON / API / Memory / GUI / Python
是行业通用术语，中文语境下已是约定俗成，强行翻译反而降低可读性和检索性
（如 "ROS2" 无标准中文译名，"Memory" 译为"记忆"易与普通记忆混淆）。
因此只翻译功能性界面文字，专业词保持原样。

### 关键设计

- 语言配置独立成 `language.py`（对应 config/language.py；因 gui/ 下已有 config.py 文件，
  Windows 不允许同名文件与目录共存，故放平级文件），以后切语言只改这一个文件
- 模式选择改用 `radio` 的下标判断（`is_local = mode_idx == 1`），不再依赖显示文字
  （如 `mode.startswith("Local")`），显示文字可随意改而逻辑不受影响
- 注意：Streamlit 右上角内置菜单（Deploy this app… 等）是框架自带界面，无法自定义翻译

### 验证结果

- `python -m py_compile` 全部通过
- AppTest 冒烟测试：页面渲染 0 异常
- ROS2 通信逻辑 / AI 调用逻辑 / 项目结构均未改动

## 2026-08-04 V5.2 第一步：Agent 层架构设计（设计完成，未写代码）

### 目标

用户输入自然语言 → Agent 理解任务 → 生成任务计划 → 选择工具 → 调用已有模块 → 返回执行结果。

### 设计要点

1. **可解释 Plan**：不展示 LLM 完整思考链，只展示「任务分析 / 目标 / 执行步骤 / 当前状态」
2. **四工具**：memory_tool（记忆）、robot_tool（动作）、vision_tool（视觉）、environment_tool（环境）
3. **V1-V4 零修改**：llm.py / memory.py / robot.py / ROS2 节点 / Gazebo 全部不动，新增 agent/ 层
4. **双后端**：robot_tool 本地模式复用 robot.py，ROS2 模式发布 /ai_robot/action 驱动现有 robot_controller
5. **双链路共存**：V3/V4 原链路（task_cli → ai_brain）保留，V5.2 Agent 走 GUI 对话区 / agent_cli

### 文件规划

- `agent/core.py`（Agent 主类）、`agent/planner.py`（DeepSeek/Mock 计划生成）、
  `agent/executor.py`（计划执行）、`agent/context.py`（会话上下文）、`agent/plan_schema.py`（Plan 结构）
- `agent/tools/`：base.py（接口）+ memory_tool / robot_tool / vision_tool / environment_tool
- `agent/agent_cli.py`（终端入口，独立测试用）

### 数据流

```
用户输入 → context 更新 → planner 生成 Plan → executor 按步骤调工具
  → memory/robot/vision/environment 四工具 → V1-V4 已有模块
  → 汇总结果 + 当前状态 → GUI 展示 Plan 与执行结果
```

### Git 提交建议

- 本步（仅文档）：`docs(agent): V5.2 agent layer architecture design (plan/tools/dataflow)`
- V5.2 完成时打 tag：`v5.2-agent`

## 2026-08-04 V5.2 第二步：agent 层代码实现（完成）

### 本次完成的功能

1. 新增 `agent/` 包：core（Agent 主类）、planner（DeepSeek/Mock 可解释 Plan）、
   executor（按步骤执行）、context（会话上下文 + 指代消解）、plan_schema（Plan 结构）
2. 新增 `agent/tools/`：base 接口 + memory_tool / robot_tool / vision_tool / environment_tool
3. `gui/ros2_client.py` 新增 `publish_action`（发布 /ai_robot/action），复用同一 ROS2 客户端
4. `agent/agent_cli.py` 终端入口 + `agent/test_agent.py`（本地冒烟）+ `agent/test_agent_ros2.py`（ROS2 集成）

### 关键设计

- **可解释 Plan**：统一结构 task_analysis / goal / steps / current_state，LLM 只输出计划不输出思考链
- **工具化**：BaseTool 统一接口（name/description/run），Agent 组装依赖注入
- **双后端**：robot_tool 本地模式复用 SimRobot，ROS2 模式发布 action 驱动现有 robot_controller
- **指代消解**：context 记录上一操作对象，"那个零件"自动替换为上一轮对象
- **临时数据库**：测试用 tempfile，不污染真实记忆库

### 验证结果

- 本地冒烟（Mock + SimRobot）：扫描/定位/移动/记住/指代/多轮上下文/error Plan 全部通过
- ROS2 集成（Mock + 真实 robot_controller）：任务 → Plan → /ai_robot/action → 状态回传 ✅
- V1-V4 代码零修改（llm.py / memory.py / robot.py / ROS2 节点 / Gazebo 均未动）

### Git 提交建议

- 本步提交：`feat(agent): agent core with planner/executor/context/four tools (V5.2 step 2)`

## 2026-08-04 V5.2 第三步：GUI 集成 Agent（完成）

### 本次完成的功能

1. `gui/backend.py`：Ros2Backend 新增 `handle_task()`，内部创建 Agent 并复用同一个 ROS2 客户端
   （Agent 支持注入 ros2_client，避免创建第二个节点）
2. `gui/app.py`：对话区改为走 Agent——展示「任务分析 / 目标 / 执行步骤 / 当前状态」+ 每步工具结果
3. `gui/language.py`：新增 Agent Plan 展示相关文案（任务分析/目标/执行步骤/当前状态/工具/执行结果/总结）
4. 新增 `gui/test_agent_backend.py`（后端 + Agent 集成测试）

### 关键设计

- Agent 可注入外部 ros2_client：GUI 只有一个 rclpy 节点，Agent 与状态面板共享数据源
- 对话展示分为两条消息：可解释 Plan + 执行结果（每步 ✅/❌），不展示 LLM 思考链
- V3/V4 原链路（task_cli → ai_brain）保留，GUI 走新 Agent 链路

### 验证结果

- GUI 后端 + Agent 集成：任务 → Plan → /ai_robot/action → robot_controller → 状态回传 ✅
- AppTest 冒烟测试：页面渲染 0 异常
- V1-V4 代码零修改

### Git 提交建议

- 本步提交：`feat(gui): agent-driven chat with interpretable plan display (V5.2 step 3)`

## 2026-08-04 V5.2 完成（v5.2-agent）

### 最终验证

- 本地冒烟（Mock + SimRobot）：全绿
- ROS2 集成（Agent → /ai_robot/action → robot_controller）：红色零件移动到检测区 ✅
- GUI 后端 + Agent（handle_task + 可解释 Plan）：通过
- AppTest 页面渲染：0 异常
- V1-V4 代码零修改（llm.py / memory.py / robot.py / ROS2 节点 / Gazebo 未动）

### Tag

- `v5.2-agent`（V5.2 完成点）

### 下一步

- V5.3：RAG 语义记忆（向量检索）

## 2026-08-04 V5.3 第一步：RAG 语义记忆设计（设计完成，未写代码）

### 目标

记忆从关键词匹配升级为语义检索：写入时生成向量，查询时用相似度召回，
关键词检索保留作兜底（混合检索）。

### 设计要点

1. 嵌入模型：`bge-small-zh-v1.5`（中文小模型 ~100MB，CPU 推理）
2. 框架：sentence-transformers（需装 torch CPU 版，~500MB，磁盘充足）
3. 向量存储：SQLite 新表 `memories_embeddings` + numpy 余弦检索（不引入向量数据库）
4. 模型来源：ModelScope 下载到 `~/.ai_robot/models/`（国内可用）
5. 降级策略：模型不可用时自动回退 V2 关键词检索，功能不阻塞
6. V1-V4 零修改：memory.py / llm.py / robot.py / ROS2 节点 / Gazebo 不动

### 文件规划

- `agent/rag/embedder.py`（嵌入封装）、`vector_store.py`（向量表+余弦检索）、
  `retriever.py`（混合检索）、`model_download.py`（ModelScope 下载）
- 集成：memory_tool（写同步向量 + 语义查询）、planner（上下文用 retriever）、
  GUI 记忆页（显示相关度）、core（组装 + 自动降级）

### Git 提交建议

- 本步（仅文档）：`docs(agent): V5.3 RAG memory design`
- 实现：`feat(agent): RAG semantic memory (embedder/vector store/hybrid retrieval)`
- V5.3 完成时打 tag：`v5.3-rag`

## 2026-08-04 V5.3 第二步：RAG 基础模块实现（完成）

### 本次完成的功能

1. 新增 `agent/rag/`：config.json（模型名/设备/路径/来源配置化）+ config.py（配置加载）
2. `embedder.py`：sentence-transformers 懒加载 + 归一化向量
3. `vector_store.py`：SQLite 新表 memories_embeddings + numpy 余弦检索 top-k
4. `retriever.py`：混合检索（语义为主 + 关键词兜底，带 source 标记）
5. `model_download.py`：ModelScope 自动下载 bge-small-zh-v1.5（失败回退 HF 镜像）
6. `test_rag.py`：语义相近用例测试

### 测试结果

- 记忆「红色零件位于右侧区域」，查询「那个红色东西在哪里」→ **top1 正确召回红色零件** ✅
- 关键词兜底（无模型）：「红色零件」→ 正确返回，来源标记为"关键词检索"

### 环境踩坑（重要）

- torch CPU wheel 国内下载：清华 pytorch-wheels 已 404，上交镜像索引实际指向官方 CDN
  （download-r2.pytorch.org），WSL 直下超时 → 改为 Windows 侧下载（1.9MB/s）后
  pip install 本地 wheel，依赖走清华 PyPI
- 系统自带旧版 Pillow 9.0.1 / scipy 1.8.0 与 transformers 5.x 冲突
  （PIL.Image 无 Resampling）→ `pip install -U pillow scipy` 强制升级
- numpy 保持 1.26.4 不受影响

### Git 提交建议

- 本步提交：`feat(agent): RAG semantic memory base (embedder/vector store/hybrid retriever)`
