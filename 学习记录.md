# AI-Robot-Demo 学习记录

## 2026-08-03 第一阶段：搭建并跑通 AI 机器人大脑

### 本次完成的功能

1. 项目骨架：main.py / llm.py / robot.py / README.md / requirements.txt / .env
2. DeepSeek 真实模式 + 离线 Mock 模式，启动时自动切换
3. 桌面快捷方式"AI机器人Demo"，双击启动
4. 接入真实 DeepSeek API Key，口语化指令测试通过

### 核心原理（理解而不是只会运行）

- **三层架构**：理解层（llm.py）→ 契约层（JSON）→ 执行层（robot.py）。
- **为什么用 JSON**：自然语言是模糊的，机器只能执行确定的结构化指令。JSON 就是人和机器之间的"翻译契约"，这也对应真实机器人中的消息格式（如 ROS2 的 msg）。
- **Prompt 设计是关键**：system prompt 限定了 AI 的角色、动作集合、输出格式，让大模型从"聊天"变成"可编程工具"。response_format 强制输出 JSON，temperature 降低随机性。
- **接口一致才能替换**：MockPlanner 与 DeepSeekPlanner 接口相同（都是 plan_task），所以没有 Key 也能测试；未来 robot.py 换成 ROS2 控制器时，main.py 也不用改。
- **世界模型**：robot.py 里的 workspace 字典就是机器人的"世界状态"，机械臂位置和夹爪是状态变量——这是机器人编程的核心思想。

### 修改记录

| 时间 | 提交 | 说明 |
| --- | --- | --- |
| 2026-08-03 | 3442240 | first AI robot demo |
| 2026-08-03 | a9b039f | add desktop launcher and auto mode detection |
| 2026-08-03 | 2b5a463 | feat: V2 memory system（Memory 记忆系统） |

### 验证结果

- `把红色零件移动到检测区` → `{"action": "move", "object": "红色零件", "target": "检测区"}` 执行成功
- 口语化指令 `帮我把那个红色的零件拿到做检测的地方去` → DeepSeek 正确理解并规划成功

### 下一步

- 上传 GitHub ✅（已推送）
- 阶段 2：给机器人加记忆（状态持久化）和环境信息 ✅

## 2026-08-03 第二阶段：Memory 记忆系统（V2）

### 本次完成的功能

1. `memory.py`：SQLite 记忆系统（保存 / 查询 / 格式化给 AI）
2. `main.py` 新流程：用户输入 → 记忆查询 → 记忆 + 任务发给 DeepSeek → 动作 → 执行
3. `llm.py`：system prompt 升级为带 `{memory}` 记忆上下文的模板
4. `robot.py`：新增 `add_station()`，让机器人学会记忆中的新位置
5. `database.db`：自动生成，记忆跨会话持久化

### 核心原理（理解而不是只会运行）

- **SQLite 是什么**：轻量级嵌入式数据库，所有数据存在单个 `.db` 文件里，Python 内置 sqlite3 模块直接操作，不需要安装数据库服务。适合单机小项目。
- **记忆检索怎么做**：用 `LIKE` 模糊匹配主题和内容，还支持"提问里包含主题"的反向匹配，所以 `把零件送到A区域` 能查到 A区域 的记忆。
- **AI Agent 为什么需要 Memory**：大模型本身没有记忆，每次对话都是独立的。记忆 = 外部存储 + 上下文注入，让 AI 结合历史知识和环境信息做决策。
- **上下文注入原理**：把查询到的记忆格式化成一两行文本，塞进 system prompt 的 `{memory}` 位置，AI 就"知道"环境了。
- **架构意义**：V2 形成了 Agent 的完整一环：记忆（Memory）→ 规划（LLM）→ 执行（Robot）。未来 ROS2 阶段，记忆层可以变成传感器/环境信息的存储和查询。

### 验证结果

- `记住：A区域在生产线左侧` → 保存成功，机器人学会新位置
- `把零件送到A区域` → 检索到记忆，AI 生成 `move` 指令，机器人执行成功
- 关掉程序重新打开 → 机器人自动"回忆起"A区域（数据库持久化）
- V1 旧指令（移动/扫描/放置）全部回归通过
